[project]
name = "mongo-challenge"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "divya-2006", email = "divyachittala82934@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "pymongo>=4.18.1",
]

[project.scripts]
mongo-challenge = "mongo_challenge:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
